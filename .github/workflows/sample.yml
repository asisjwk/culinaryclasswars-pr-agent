name: "Actions-Connection-Check"

on:
  pull_request:
    types: [opened, synchronize]
  workflow_dispatch:

jobs:
  ping-test:
    runs-on: ubuntu-latest
    steps:
      - name: "Hello World"
        run: |
          echo "GitHub Actions is working!"
          echo "Event Name: ${{ github.event_name }}"